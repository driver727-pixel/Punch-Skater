/** Small badge displayed in the header of every admin-only page. */
export function AdminPageBadge() {
  return (
    <span className="admin-page-badge" aria-label="Admin only page">
      🔐 Admin Only
    </span>
  );
}
